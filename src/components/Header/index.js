import React from "react";

export default function Header() {
  return (
    <header>
      <img src="assets/logo.png" alt="Logo" />
      <nav>
        <a href="#">PRODUTOS</a>
        <a href="#">SOBRE</a>
        <a href="#">CONTATO</a>
      </nav>
    </header>)
}